/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';

// Register using the native component name defined in MainActivity.getMainComponentName()
AppRegistry.registerComponent('Bilhetinho', () => App);
